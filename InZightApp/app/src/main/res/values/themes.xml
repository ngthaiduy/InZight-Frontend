<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.InZightApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>
    <style name="DialogStyle" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.5</item> <!-- Nền mờ -->
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:windowElevation">8dp</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="Theme.InZightApp" parent="Base.Theme.InZightApp" />

    <style name="Theme.InZightApp.Calendar" parent="Theme.MaterialComponents.Light">
        <item name="colorAccent">@color/home_primary</item>
        <item name="android:textColorPrimary">@color/home_text_strong</item>
    </style>
</resources>